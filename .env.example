# Combined environment example for LuminAI Genesis
# This merges top-level `.env.example`, bot-specific placeholders, and extra third-party keys.
# Use this as a reference; do NOT commit real secrets to the repo. Copy to `.env.local` and fill real secrets there.

# General
DEBUG=false
ENVIRONMENT=development
LOG_LEVEL=debug

# Stripe / Payments
# Set these in your local `.env.local` when testing. Do NOT commit secrets.
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=

# Calendly
CALENDLY_URL=https://calendly.com/elidorascodex
CALENDLY_API_KEY=

# Email (for confirmation emails)
EMAIL_BACKEND=smtp  # 'smtp' or 'sendgrid'
EMAIL_HOST=smtp.gmail.com  # For SMTP: Gmail, SendGrid, etc.
EMAIL_PORT=587
EMAIL_USERNAME=your-email@gmail.com  # For SMTP
EMAIL_PASSWORD=app-password  # For SMTP (use app password, not account password)
EMAIL_FROM=noreply@luminai-genesis.local  # Sender address
SENDGRID_API_KEY=  # Alternative to SMTP; if set, takes precedence

# Web / App
WEBHOOK_HOST=http://localhost:8000  # public host or ngrok/stripe-cli forwarding URL for webhooks
NODE_ENV=development

# GitHub
GITHUB_TOKEN=
GITHUB_PERSONAL_TOKEN=

# Discord
DISCORD_WEBHOOK_URL=
DISCORD_BOT_TOKEN=

# Bitwarden
# (for local retrieval) - keep multiple identifiers if used by tooling
BW_CLIENTID=
BW_CLIENTSECRET=
BW_CLIENTSECRETID=
BW_CLIENT_KEY=

# AI / model keys
OPENAI_API_KEY=
OPENAI_ORG_ID=
CLAUDE_API_KEY=
XAI_API_KEY=
TEC_OLLAMA_API_KEY=
TEC_RUNWAY_API_KEY=
TAVILY_API_KEY=

# Other third-party keys
WORLDANVIL_API_KEY=
SPOTIFY_CLIENT_ID=
SD_API_KEY=
SD_ORG_ID=

# Hosting / infra
WPCOM_SSH_SECRET_KEY=
WPCOM_API_PASS=
PROJECT_TOKENS=

# Git / CI
GITLAB_PAT=
BE_CLIENTID=

# Misc
PROJECT_TOKEN=

# Local-only/testing flags
LOCALSTACK_ENABLED=false

# Local-only secrets (do NOT commit real values to repo) -> put these in .env.local
NGROK_AUTHTOKEN=
WEBHOOK_SECRET=
