apiVersion: v1
kind: Service
metadata:
  name: luminai-backend
  labels:
    app: luminai-backend
spec:
  type: ClusterIP
  selector:
    app: luminai-backend
  ports:
    - name: http
      port: 80
      targetPort: 8000
